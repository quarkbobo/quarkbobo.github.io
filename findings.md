# Findings & Decisions

## Requirements
- 完整备份网站当前所有内容。
- 删除/替换现有 NexT 主题。
- 重新生成高审美科幻个人网页。
- 使用深色星空背景、蓝紫色土星和自然表面流光。
- 粒子按用户视频中的弧形流场运动，并保持平滑高帧率。
- 鼠标经过时触发克制的视差、聚散或高亮交互。
- 保留现有文章、小游戏、工具与下载文件。
- 将“上传至 GitHub”和“更新目录”的两个本地入口合成一个快捷入口。
- 三个技能现均位于 `C:/Users/Lenovo/.codex/skills`，且 CLI 可发现：frontend-design、web-design-guidelines、vercel-react-best-practices。
- 主题对外名称确定为“流体粒子”，兼容目录名为 `themes/fluid-particle`。

## Research Findings
- 项目是 Hexo 8.1.1，当前主题配置为 `next theme`。
- 内容主要位于 `source/_posts`，另有国际象棋、中国象棋、贪吃蛇、2048、图像转换器等静态页面。
- Git 当前分支为 `master`，远端为 `origin/master`，开始检查时工作区干净。
- 桌面存在 `BoBo一键更新.lnk`，执行 `git add . && git commit -m 'update' && git push --force origin master`；强制推送存在风险。
- 桌面另有 `Posts.lnk` 和 `files.lnk`，分别打开文章目录与文件目录；用户描述的“更新目录”尚不能唯一对应。
- 参考视频为 1280×592、30 FPS、8.1 秒 HEVC 视频。
- 连续帧显示：主体画面基本静止，运动集中在下半部；大多数粒子短距离缓慢漂移，少量高亮粒子快速穿过。
- `source/_posts/博客目录.md` 的 `permalink: /` 占用根路由；新主题必须把该根文章视为首页数据源，不能移动它或更改 URL。
- 现有依赖已包含 `hexo-renderer-ejs`，可直接创建 EJS 主题，无需增加运行时 npm 依赖。

## Technical Decisions
| Decision | Rationale |
|----------|-----------|
| 使用独立自有主题，不直接魔改 NexT | 更容易形成一致视觉系统，也能保留 Hexo 内容生成能力 |
| Canvas 只绘制粒子，土星结构优先用 CSS/轻量图层 | 控制重绘成本，兼顾清晰度和响应式 |
| 粒子按真实时间推进而非逐帧固定步长 | 降低掉帧时的速度跳变和机械感 |
| 使用预渲染粒子光点贴图 | 避免每颗粒子每帧重复计算阴影模糊 |
| 普通流尘与稀有高速粒子拆分模型 | 更接近参考视频的节奏层次 |
| `post.ejs` 对 `page.path === 'index.html'` 使用首页局部模板 | 保留博客目录文章的根 URL，同时实现新的首页布局与文章查询 |

## Issues Encountered
| Issue | Resolution |
|-------|------------|
| 技能目录不止三个，无法从时间戳确定用户所指 skills | 请求用户提供准确名称或截图 |
| 浏览器测试标签的 FPS 被后台节流 | 正式实现后使用前台页面、不同视口和真实设备验证 |
| `web-design-guidelines` 未实际出现在工作区或全局技能清单 | 本轮无法读取；以键盘焦点、对比度、语义结构、响应式、reduced-motion 和性能预算作为显式验收项 |
| Vercel 技能的仓库目录名与技能名不同 | 使用 `skills/react-best-practices` 并指定目标名 `vercel-react-best-practices`；网页规范位于 `skills/web-design-guidelines` |
| 系统 Python 的 urllib HTTPS 触发 OpenSSL Applink 错误 | 安装器切换为受支持的 Git 下载模式，无需改系统 SSL |

## Resources
- `C:/Users/Lenovo/Desktop/Quarkbobo/_config.yml`
- `C:/Users/Lenovo/Desktop/Quarkbobo/themes/next theme`
- `C:/Users/Lenovo/Documents/xwechat_files/wxid_uz6uj3dkp9t112_c034/msg/video/2026-08/b26aab7263f557c6aabc9389993c3049.mp4`

## Visual/Browser Findings
- 用户选择了“深空档案馆”方向：左侧大标题与内容，右侧单一主天体，整体克制。
- 主色为蓝紫，球面需有气态条带与表面流光。
- 土星环必须有前后遮挡层次，不能只是细椭圆描边。
- 参考粒子从左下沿多层弧线向右侧天体区域流动，亮暗与速度层次明显。
- 鼠标交互应是轻微扰动和视差，不应改变整体流向。
- 用户最新覆盖此前暖色矿物纹理：球面应保持低色差蓝紫气态色谱，相邻云带只做细微明度/色相变化。
- 局部特征不是耀斑或独立旋涡，而是一股近垂直气流压入表面；接触点附近的共享纹理坐标场向四周外扩，外围仅有少量 curl 形成卷吸和混沌。
- 初始化阶段的确定性域扭曲可复用现有纬向差速产生自然剪切；把动态二维流场放入每帧投影会使约 3.37ms 的热循环上升到约 4.18ms，越过 p95 4ms 风险线，因此不采用。

## 2026-08-31 真实行星参考图分析
- 用户明确要求删除日珥、火焰舌和恒星冠层，把主体变成看得出球面的真实行星。
- 第一张参考图的有效特征是固定球面明暗、受光侧细薄青蓝大气边缘、深暗夜面，以及星环的前后遮挡；图片水印和编号仅是来源标记，不能使用。
- 第二张参考图的有效特征是木星式横向矿物流纹：暖沙、陶土玫瑰、矿物紫与深海蓝不规则交叠；外围水彩泼溅不适合当前深空首页。
- 推荐分层：后半星环 → 圆形裁切球体 → 多层倾斜云带/矿物流纹 → SVG 噪声扰动 → 固定球面光照 → 细薄大气缘光 → 前半星环。
- 真实感关键：光照、暗面和缘光保持静止，只有内部纹理以 55–80 秒周期缓慢横移；避免整颗球连同高光一起旋转成“圆形贴纸”。
- 建议色彩：深空 `#070914`、夜面 `#17132C`、深海蓝 `#163A57`、矿物紫 `#563459`、陶土玫瑰 `#B45F68`、暖沙 `#F0D3B1`、大气青 `#68D9F4`。
- 星环应比现状更克制：暗靛蓝/灰蓝实体带、极细青光尘边，不做白色粗光圈或冲击波。
- 保留现有背景粒子流，不把参考图本身或任何水印当作网页资产。

## 2026-08-31 当前行星实现审查
- 当前 DOM 把日珥做成独立 `saturn-prominences` SVG（5 个双描边路径），球内还有 `saturn-flares` 发光斑；两者都应完整删除，而不是仅设为透明。
- 当前 `saturn-bands` 已经具备两层无缝横移结构，可保留动画机制，但现有色带偏青紫霓虹、周期 26/42 秒，纹理过平滑且移动偏快，导致更像高能气体而非实体行星。
- 当前 `saturn-light` 是固定光照层，方向正确；需要改为更有体积的受光侧高光、暗夜面和窄大气缘光组合。
- 当前星环已经拆成 back/front 两层并共享 `--saturn-equator-angle: -10deg`，前后遮挡结构可直接保留；需加宽并降低亮度/霓虹感。
- 当前截图的主要问题与用户反馈一致：球体是光滑紫色渐变，表面纹路不够自然；右侧五条外部发光弧线让它更像恒星或能量装置。
- 背景粒子、首页文案布局、暂停按钮与 Canvas 合同均不需要改变。
- 现有测试专门要求 5 个日珥、flare 和相关动画，实施时必须先将契约改为“无日珥 + 有多层真实纹理 + 固定球面光照 + 同轴星环”，并验证 RED 后再改生产代码。
- 用户确认星环采用更宽、更暗、更真实的尘埃结构，只保留一圈克制的青蓝边光。
- 用户选择球面配色 C：冷蓝紫夜面为基底，暖沙与陶土色云带构成主要实体纹路。
- 用户在实现方式中选择独立 Canvas 实时生成球面表面，不采用纯 CSS 或 CSS + SVG 噪声方案。

## 2026-08-31 已批准设计部分
- 视觉结构已批准：删除全部日珥/耀斑；独立球面 Canvas 生成冷蓝紫夜面、暖沙陶土云带、暗裂隙和一处克制旋涡；固定 CSS 光照覆盖；宽暗尘埃环仅留细青蓝受光边。
- 运动与性能已批准：预生成无缝纹理，运行时只推进经度相位；完整转动约 70 秒，深层纹理差速；整体页面保持高刷新，球面根据成本自适应更新；暂停、后台、reduced-motion 和失败时均保留静态帧。
- 独立审查建议模块化为 `planet-core.js`（确定性纹理/映射）与 `planet-surface.js`（DOM 生命周期/尺寸/暂停/降级），不得修改粒子核心与渲染器。
- 差速采样必须保持未取模的赤道基准相位，先乘纬度速度因子再对 `2π` 取模，避免高纬纹理每圈倒跳。
- 视觉布局与性能策略统一使用 `760px` 断点；`768×1024` 明确保留右侧构图，`760px` 及以下才移到右下方。
- Canvas 初始化期间和失败时保留 CSS 静态底图；自身失败使用 `.planet-fallback`，不能误用 `.particle-fallback`。
- 用户确认第三部分的响应式、双 Canvas 首页合同、自动测试、四档视觉检查、前台性能验收与无外部参考图资产；随后澄清继续使用方案 C。

## 2026-08-31 实施计划复审结论
- 实施拆为 `planet-core.js` 与 `planet-surface.js`：前者负责确定性 1024×512 纹理、球面映射、未取模相位与质量策略，后者只负责第二个 Canvas 的 DOM 生命周期、暂停/可见性、尺寸、降级与指标。
- 正式 20 秒性能验收使用只读 `mark()/measureSince()` 对调用者选定区间内的每次成功绘制做精确统计，不能用最后一个 120 帧质量窗口代替完整样本。
- 质量级变化必须先用 `backing.fps` 更新目标重绘频率，再判断尺寸是否相同；否则小尺寸 Canvas 会出现分辨率未变、FPS 也错误未变的问题。
- 最终保护范围除粒子 JS 哈希外，还显式验证 `source/`、依赖清单、`_config.yml`、`render.yaml` 与 `tools/` 均未改变。
- 最终独立复审确认计划无提交级阻塞；计划提交为 `91bc91e`。

## 2026-08-31 执行预检
- 隔离分支基线为 99/99 测试通过，粒子核心与渲染脚本哈希精确匹配，说明现有粒子效果可作为可靠保护基线。
- 计划的主要测试仍是可观察行为测试（纹理连续性、缓冲区复用、Canvas 生命周期、Chrome 计算样式与布局、真实前台性能）；源码正则和哈希不是主测试。
- 但计划同时强制少量源码/change-detector 断言，用于热循环零分配、旧日珥符号彻底删除、粒子文件字节不变；这与 `writing-good-tests` 的严格“行为而非源码文本”规则存在明确政策冲突。
- 推荐裁决：把这些静态检查保留为窄范围补充防线，行为/浏览器测试继续作为主要证据；若用户不接受，则先修订计划，把哈希移至独立验证门并尽量将模板/CSS源码断言改为构建后 DOM/计算样式测试。
- 用户最终选择 A，以上推荐成为本次实现的测试政策；后续审查必须拒绝把源码正则或哈希当作功能正确性的唯一证据。

## 2026-08-31 Task 2 审查经验
- 范围/类型断言不足以证明球面投影正确：倾角符号、纬度差速、双线性采样、limb alpha 与透明区隔离均需要可手算的人工 fixture。
- 新的 mutation gate 已证明测试能拒绝固定差速、忽略倾角、常量 sampler 与伪 redraw；后续渲染器测试继续优先检查可观察像素和生命周期，而不是只检查符号存在。
- `frontend-design` 校准后继续执行已批准视觉：深空个人档案馆的签名元素只有真实冷暖气态行星与宽暗尘埃环；现有字体、文案和信息结构保持安静，不添加通用 HUD 或额外装饰。

## 2026-08-31 仓库目录清理初步审计
- 仓库顶层同时包含站点源码、测试、设计文档、本地规划日志、独立 `tarot-reigns` 子项目，以及 Hexo 可再生目录。
- `node_modules/`（约 50.80 MB）、`public/`（约 1.34 MB）和 `db.json`（约 0.13 MB）均由 `.gitignore` 排除，属于可重建产物；删除后可通过安装依赖和 Hexo 构建恢复。
- `.worktrees/` 当前为空且被忽略，是此前隔离开发留下的容器目录，可作为低风险清理候选。
- 初次使用 Git 默认转义输出时看到了 `"source`，复核 `core.quotePath=false` 后确认这只是中文路径的 C 风格显示，不存在额外的带引号目录；正常 `source/` 共 61 个受跟踪路径。
- `tarot-reigns/` 约 0.21 MB，拥有自己的 `src/`、`tests/`、`docs/`、`requirements.txt`，更像独立项目；在用户确认是否仍需保留前不移动或删除。
- `desktop.ini`、`_config.landscape.yml`、`.codebuddy/settings.local.json` 均被 Git 跟踪，需要逐项确认用途后再决定。
- `_config.landscape.yml` 是空文件，`themes/landscape` 不存在，当前主题明确为 `fluid-particle`；`hexo-theme-landscape` 与其 Stylus 渲染依赖是高置信度的遗留候选，但移除依赖后仍需 fresh build 验证。
- `render.yaml` 指向不存在的 `xiangqi-server/`，当前配置无法生效；若用户已不再使用 Render 象棋服务，可删除，否则应迁回对应独立项目。
- `.codebuddy/settings.local.json` 只保存本地编辑器插件开关，站点、测试和工具均无引用，适合取消跟踪并加入忽略规则。
- `desktop.ini` 属于 Windows 文件夹元数据，站点无引用，适合从仓库删除并忽略。
- `themes/.gitkeep` 在 `themes/fluid-particle/` 已存在后失去占位作用，是可直接删除的低风险文件。
- `tarot-reigns/src/**/__pycache__/*.pyc` 被 Git 错误跟踪，属于确定可再生的 Python 字节码；无论保留还是迁移子项目，都应删除并通过 `.gitignore` 防止回归。
- 设计前已有完整外部备份 `C:/Users/Lenovo/Desktop/Quarkbobo-backups/Quarkbobo-before-redesign-20260828-211820`，并有逐文件哈希记录；清理不需要用仓库内缓存充当恢复点。
- `tools/quark-blog-tools.ps1` 与对应测试仍是用户桌面快捷入口的核心，且内部固定引用项目根与 `source/_posts`，必须保留原位置或同步更新测试/快捷入口，不能当作杂项删除。
- 全部受跟踪文件的 SHA-256 分组未发现非空内容完全重复；`source/files/backup/` 不是主目录文件的简单字节级副本，不能按“重复文件”自动删除。
- `tarot-reigns/` 不被 Hexo 配置、主题、工具或测试引用；它可以独立迁出博客仓库，但是否保留属于用户内容决策。
- 用户选择将 `tarot-reigns/` 迁出 `Quarkbobo`，作为桌面独立项目保留；实施时必须先复制/移动并核验文件清单与哈希，再从博客仓库清除路径。
- 用户选择继续保留 `source/files/backup/` 并随博客发布；该目录不属于清理范围，现有下载 URL 必须保持稳定。
- 用户选择保留开发记录并统一归档；推荐把根目录规划日志和 `docs/superpowers`、`docs/verification` 整理到 `docs/development/` 的明确子目录，`docs/recovery/` 独立保留。
- 用户选择总体“安全整理”方案：不重构稳定的 `source/`、`test/`、`tools/` 路径，只处理独立项目、文档归档、失效配置和可再生产物。
- 桌面目标 `C:/Users/Lenovo/Desktop/TarotReigns` 当前不存在，可作为无冲突迁移目标。
- 仓库中没有任何 `.styl` 源文件；`hexo-theme-landscape` 和 `hexo-renderer-stylus` 均只是根项目直接依赖，没有其他包引用，适合成对移除并更新 lockfile。
- 文档迁移后的路径引用主要集中在根 `progress.md`、两份历史实施计划和本次清理规格；实施时更新仍需导航的链接，保留本清理规格中的“旧路径 → 新路径”迁移说明。
- `source/files/backup/` 当前恰有 12 个受保留下载文件；结构合同可记录精确文件名，防止整理误删或改 URL。
