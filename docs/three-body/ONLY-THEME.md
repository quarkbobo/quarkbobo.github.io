# 单一三体外观

用户最新要求“原来的那个主题不要”，替代原先保留经典切换入口的要求。沿用已授权的发布流程。

选择最小方案：初始HTML直接声明三体外观，删除经典切换按钮及运行时切换分支，旧本地偏好与URL主题参数不再决定外观；首页三体场景默认显示，归档与正文延续三体阅读配色。旧星球运行入口在生命周期接缝处停用，避免创建第二个WebGL场景。保留原文、URL、共享布局及历史源码资产，避免跨白名单删除文件。

顺序：新增真实默认入口验收并保存红灯；实现单一外观；重建运行209项原Node测试；执行桌面/手机、旧偏好、旧链接、暂停/单步、滚动冻结、归档/文章的新浏览器检查；提交并发布，确认Actions与线上默认根路径。

旧只读工具中“经典默认、切回经典”的断言与新要求相冲突，原件及历史报告不改，不伪造旧场景ready状态。新检查验证当前单一主题约定；旧物理断言、参数及数值阈值保持原样。旧域名/图标问题仍按BLOCKED记录，不扩大本轮范围。
